# Copied from Elemental2, modified to include more externs.
# If this proves helpful, will see if we can share it between repos.

package(default_visibility = ["//visibility:public"])

licenses(["notice"]) # Apache2

exports_files(glob([
    "externs/**/*.js",
    "contrib/externs/*.js",
    "contrib/externs/**/*.js"
]))